# 2026-08-31 플러그인 README 설치 안내 정리

## 완료 내용

- 루트 README의 플러그인 목록을 현재 로컬 마켓플레이스에 남아 있는 `news2shorts`, `cc-helper`, `shorts-suite`, `careerfill` 기준으로 정리했다.
- 각 플러그인의 주요 용도와 기본 안전 경계를 비교할 수 있도록 표로 정리했다.
- `news2shorts-local` 마켓플레이스 등록, 개별 플러그인 설치, 설치 상태 확인 명령을 추가했다.
- 설치 또는 업데이트 후 새 Codex 작업에서 변경된 Skill과 도구를 불러와야 한다는 안내를 추가했다.
- API 키와 토큰을 저장소에 기록하지 않고 환경 변수 또는 운영체제 보안 저장소로 전달하도록 명시했다.

## 확인 범위

- 현재 브랜치의 플러그인 마켓플레이스 등록 상태와 네 플러그인의 manifest를 기준으로 문서를 맞췄다.
- `news2shorts-local` 마켓플레이스 이름 검증과 네 플러그인의 구조 검증을 통과했다.
- 커밋 후보의 비밀키·API 키·토큰·URL 자격 증명·개인식별정보·고위험 인증 파일 패턴을 검사했으며 실제 민감정보는 발견되지 않았다.
- `git diff --check`를 통과했다.
- 프론트엔드 빌드는 저장소 작업 지침에 따라 실행하지 않는다.
