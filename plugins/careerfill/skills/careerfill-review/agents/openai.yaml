interface:
  display_name: "CareerFill 초안 검토"
  short_description: "지원서 초안의 근거·제한·중복·민감 항목을 점검합니다."
  default_prompt: "Use $careerfill-review to audit this CareerFill application session and list every source, length, duplication, and safety blocker."
policy:
  allow_implicit_invocation: true
